const [a, b, c, d] = [3, 5, 1, 15]

const soma = a + b + c + d
const subtracao = a - d
const multiplicacao = d * b
const divisao = d / a
const modular = a % 2

console.log(soma, subtracao, multiplicacao, divisao, modular)

